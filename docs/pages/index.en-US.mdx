---
title: 'livepeer.js: Livepeer for JS'
searchable: false
---

import { Tabs, Tab } from 'nextra-theme-docs';
import { Header } from '../components/docs';

<Header />

<div className="text-center">
  **livepeer.js** is a library to make building with Livepeer simple. It provides a core vanilla JS library to easily connect to a
  Livepeer Media Server (e.g. Livepeer Studio), as well as React-specific hooks with a similar API to provide memoization and DOM
  management.

Instantly build a decentralized video app with the Livepeer protocol.

</div>

{/* prettier-ignore-start */}

<div className="max-w-md mx-auto mt-5 mb-8 text-center install">
  <Tabs items={['npm', 'pnpm', 'yarn']}>
    <Tab>
```bash
npm i livepeer @livepeer/react wagmi ethers
```
    </Tab>
    <Tab>
```bash
pnpm i livepeer @livepeer/react wagmi ethers
```
    </Tab>
    <Tab>
```bash
yarn add livepeer @livepeer/react wagmi ethers
```
</Tab>
  </Tabs>
</div>
{/* prettier-ignore-end */}

<div className="mb-20 text-center">
  [Get Started](/docs/getting-started) · [GitHub
  Repository](https://github.com/livepeer/livepeer.js)
</div>

## Overview

```tsx
import { createReactClient, LivepeerConfig } from '@livepeer/react';
import { studioProvider } from 'livepeer/providers/studio';

const client = createReactClient({
  provider: studioProvider({ apiKey: 'yourStudioApiKey' }),
});

function App() {
  return (
    <LivepeerConfig client={client}>
      <SomeComponents />
    </LivepeerConfig>
  );
}
```

```tsx
import { useLPMSProvider } from '@livepeer/react';

function LivepeerMediaServer() {
  const lpmsProvider = useLPMSProvider();

  return <div>Using {lpmsProvider.getLPMS().name}</div>;
}
```

In this example, we create a livepeer `Client` and pass it to the `LivepeerConfig` React Context. The client is set up to use the ethers Default Provider and automatically connect to previously connected wallets.

Next, we use the `useConnect` hook to connect an injected wallet (e.g. MetaMask) to the app. Finally, we show the connected account's address with `useAccount` and allow them to disconnect with `useDisconnect`.

We've only scratched the surface for what you can do with livepeer!

## Features

livepeer.js supports all these features out-of-the-box:

- Hooks for working with Livepeer Media Servers and the Livepeer protocol
- Caching, request deduplication, multicall, batching, and persistence
- Auto-refresh data on network changes
- TypeScript ready
- Test suite running against forked Ethereum network

...and a lot more.

## Community

Check out the following places for more livepeer-related content:

- Follow [@livepeer](https://twitter.com/livepeer) on Twitter for project updates
- Join the [discussions on GitHub](https://github.com/livepeer/livepeer.js/discussions)
